import { Observable } from 'rxjs';
declare class ActiveRequest {
    promise: Promise<any>;
    isComplete: boolean;
    value: any;
    error: Error;
    constructor(promise: any);
    setValue(value: any): void;
}
export declare class ObservablePromiseCache {
    activeRequests: Map<any, ActiveRequest>;
    constructor();
    getRequest(observable$: Observable<any>, observableId: any): ActiveRequest;
    removeRequest(observableId: any): void;
}
export {};
