import { list, object } from 'rxfire/database';
import { useObservable } from '..';
export function useDatabaseObject(ref, options) {
    return useObservable(object(ref), ref.toString(), options ? options.startWithValue : undefined);
}
export function useDatabaseList(ref, options) {
    return useObservable(list(ref), ref.toString(), options ? options.startWithValue : undefined);
}
